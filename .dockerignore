# ==============================================================================
# TenPoint — .dockerignore
#
# Build context = THƯ MỤC GỐC dự án (deploy/docker-compose.yml đặt `context: ..`).
# Mục tiêu: context nhẹ → build nhanh, cache ổn định, không lọt secret vào image.
#
# ⚠️ KHÔNG loại trừ: backend/**, frontend/** (trừ artefact), deploy/web-entrypoint.sh
# ==============================================================================

# ------------------------------------------------------------------------------
# Version control & CI
# ------------------------------------------------------------------------------
.git
.gitignore
.gitattributes
.github
.gitlab-ci.yml

# ------------------------------------------------------------------------------
# Secret — TUYỆT ĐỐI không đưa vào image
# ------------------------------------------------------------------------------
**/.env
**/.env.*
!**/.env.example
*.pem
*.key
secrets/

# ------------------------------------------------------------------------------
# Artefact của Node / Next.js — phải cài & build LẠI bên trong image
# ------------------------------------------------------------------------------
**/node_modules
**/.next
**/out
**/build
**/.turbo
**/.swc
**/.yarn
**/.pnp
**/.pnp.*
**/*.tsbuildinfo
**/.vercel

# ------------------------------------------------------------------------------
# Artefact của Go
# ------------------------------------------------------------------------------
backend/bin
backend/dist
backend/tmp
**/*.test
**/*.out
**/__debug_bin*

# ------------------------------------------------------------------------------
# Tài liệu & tài sản không cần khi build
# ------------------------------------------------------------------------------
docs
README.md
deploy/README.md
*.md
!backend/**/*.md
LICENSE
*.jpeg
*.jpg
*.png
!frontend/public/**

# ------------------------------------------------------------------------------
# File vận hành không thuộc image
# ------------------------------------------------------------------------------
Makefile
backups
deploy/docker-compose*.yml
deploy/Dockerfile*
deploy/Caddyfile
deploy/.env.example

# ------------------------------------------------------------------------------
# Test & coverage
# ------------------------------------------------------------------------------
**/coverage
playwright-report
test-results
.playwright-mcp

# ------------------------------------------------------------------------------
# Log
# ------------------------------------------------------------------------------
**/*.log
logs
**/npm-debug.log*
**/yarn-error.log*

# ------------------------------------------------------------------------------
# Hệ điều hành & IDE
# ------------------------------------------------------------------------------
**/.DS_Store
**/._*
**/Thumbs.db
**/desktop.ini
.idea
.vscode
.history
.fleet
.zed
**/*.swp
**/*~
